export const SESSION_TTL_SECONDS = Number(process.env.SESSION_TTL_SECONDS) || 60 * 60; // default 1 hour
export const START_LOGIN_TTL_SECONDS = Number(process.env.START_LOGIN_TTL_SECONDS) || 60; // default 60 seconds for start-login
